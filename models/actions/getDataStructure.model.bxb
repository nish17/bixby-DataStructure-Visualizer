action (getDataStructure) {
  type(Search)
  description (Searches for the asked data structure)
  collect {
    input (dsName) {
      type (DataStructureConcept)
      min (Optional) max (One)
    }
  }
  output (dsInfo)
}
